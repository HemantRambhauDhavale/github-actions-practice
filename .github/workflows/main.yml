- name: Scan Docker Image
  uses: aquasecurity/trivy-action@master
  with:
    image-ref: '${{ secrets.DOCKER_USERNAME }}/capstone:latest'
    format: 'table'
    exit-code: '0'
    severity: 'CRITICAL,HIGH'
