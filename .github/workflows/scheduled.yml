name: Scheduled Job

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduled workflow running"
      - run: echo "Trigger: ${{ github.event.schedule }}"
