name: PR Pipeline

on:
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/reusable-build.yml

  pr-msg:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR checks passed"
